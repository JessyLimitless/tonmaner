# PRD: 마크다운 기반 EPUB3 전자책 조판 시스템 (다책 공용)

> **개정 이력**
> - v4 (2026-09-02) — 도해를 원고에서 분리(`figures/*.svg` + `<!--figure:-->` 자리표시자).
>   앞부속에 약표제지·직접 조판 표제지 추가. 책별 `style/override.css` 실제 적용.
>   git 저장소로 전환.
> - v3 (2026-09-02) — **다책(多冊) 구조로 전환.** 조판(`style/`)과 서체(`fonts/`)를
>   프로젝트 공용으로 두고, 책은 `books/<슬러그>/` 아래에 나란히 둔다.
>   본 PRD는 이제 **개별 도서가 아니라 조판 시스템 전체**의 명세다.
> - v2 (2026-09-02) — 산출물을 **EPUB3 전자책**으로 확정. 초판(v1)의 신국판 종이책
>   PDF / WeasyPrint 파이프라인 명세를 폐기하고, 실제 구축된 pandoc 파이프라인과
>   `style/book.css`의 조판 컨셉을 기준으로 전면 재작성.
> - v1 (2026-08-29) — 신국판 152×225mm 양면 인쇄용 마스터 PDF 명세. **미구현 폐기.**

---

## 1. 프로젝트 개요

- **성격:** 한 벌의 조판 시스템으로 **여러 권의 전자책**을 찍어내는 저장소.
- **산출물:** 책마다 `books/<슬러그>/build/book.epub` — EPUB3, 리플로우
- **공유 자산 (톤앤매너):** `style/book.css` · `fonts/*.woff2` · `tools/*`
  → 조판을 한 번 고치면 **모든 책에 동시에 반영된다.** 책별로 CSS를 복제하지 않는다.
- **책별 자산:** 원고 · 메타데이터 · 표지 · 산출물

### 현재 수록 도서

| 슬러그 | 도서명 | 상태 |
|---|---|---|
| `capital-allocator` | 버핏의 자본 배치학 | 제1장 집필 완료, 나머지 골격 |

- **핵심 목표**
  - 원고 수정 시 레이아웃 수작업 없이 재빌드 한 번으로 반영.
  - **서체를 임베드**하여 리더기(리디북스·밀리·Apple Books 등)와 무관하게 동일 조판.
  - 종이책 POD는 **범위 밖**.

---

## 2. 디렉토리 구조

```text
My Soul/
├── PRD.md                    # 본 문서 — 조판 시스템 전체 명세
├── style/
│   ├── book.css              # ★ 공용 조판. @font-face 포함
│   └── specimen.md           # ★ 공용 조판 견본 원고
├── fonts/*.woff2             # ★ 공용 서브셋 임베드 서체
├── tools/
│   ├── new-book.sh           # 새 책 뼈대 생성
│   ├── build.sh              # <슬러그> → EPUB + 미리보기 + 리더
│   ├── preview.sh            # 한 챕터만 HTML로 확인
│   ├── specimen.sh           # 조판 견본 (라이트/다크 토글)
│   └── reader-template.html  # 페이지 넘김 리더 틀
├── build/specimen.html       # 공용 견본 산출물
└── books/
    └── <슬러그>/
        ├── README.md         # 이 책의 파일명 규칙·빌드법
        ├── meta/metadata.yaml
        ├── manuscript/       # 원고. 파일명 오름차순이 곧 책의 순서
        ├── figures/          # 도해 SVG. 원고에서 자리표시자로 부른다
        ├── images/           # cover.jpg|png 자동 인식
        ├── style/override.css # (선택) 이 책만의 조판 — 강조색 등
        └── build/            # book.epub · preview.html · reader.html
```

★ = 모든 책이 공유하는 톤앤매너. 여기를 고치면 전 도서에 반영된다.

### 원고 파일명 규칙

| 번호대 | 용도 |
|---|---|
| `01`, `02` | 약표제지 · 표제지 (있으면 pandoc 자동 표제지를 끈다) |
| `04` | 직접 조판한 차례 (있으면 pandoc 자동 목차를 끈다) |
| `05` | 프롤로그 |
| `00` | 프롤로그 (앞부속을 직접 조판하지 않는 책) |
| `10`, `20`, `30`… | 부 도비라 (`{.part}`) |
| `11`, `12`, `13`… | 해당 부의 장 |
| `90` | 에필로그 |

---

## 3. 조판 디자인 명세

### 3.1 컨셉

> **어법: 재무제표(ledger).** 가는 괘선, 자릿수 맞춘 숫자, 서식처럼 읽히는 워크시트.

투자서라는 주제를 장식이 아니라 **조판 문법 자체**로 드러낸다. 굵은 박스와
색면 대신 헤어라인 괘선으로 구획하고, 숫자는 tabular-nums로 자릿수를 맞추며,
실습 요소는 실제 서식(form)처럼 조판한다.

### 3.2 절대 원칙

- **절대 단위 금지.** 모든 크기·여백을 `em`으로 잡는다. 독자가 리더기에서
  글자 크기를 키워도 판면 밀도가 유지되어야 한다.
- **다크 모드 대응.** 모든 색은 `:root` 변수로 정의하고
  `prefers-color-scheme: dark`에서 재정의한다. 하드코딩 색상 금지.
- **의미를 지닌 단 하나의 색.** 먹청색 `--accent`만 강조에 쓴다.

### 3.3 색

| 변수 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| `--ink` | `#14120F` | `#DDE0E4` | 본문 |
| `--soft` | `#6E6862` | `#9AA0A7` | 보조 텍스트 |
| `--faint` | `#A8A29A` | `#5F666E` | 라벨·장식 |
| `--rule` | `#E6E2DC` | `#2B2F34` | 괘선 |
| `--rule-2` | `#C9C3BA` | `#3D434A` | 진한 괘선 (표 머리) |
| `--accent` | `#2B3A55` | `#9DB4D6` | 장 번호, 핵심 지표 |
| `--wash` | `#F7F6F3` | `#1B1E22` | 워크시트·수식 바탕 |

### 3.4 서체

**KoPubWorld** — 한국출판인회의 배포 출판 전용 서체. 전자책 임베드 허용
(재배포·판매 금지, 출처 표기 유지).

| 역할 | 서체 | 굵기 |
|---|---|---|
| 본문·장 표제 (`--serif`) | KoPubWorld Batang | Light 300 / Bold 700 |
| 소제목·라벨·표 (`--sans`) | KoPubWorld Dotum | Medium 500 / Bold 700 |

- 원고에 쓰인 글자 + 한글 음절 전체(U+AC00–D7A3) + 구두점으로 **서브셋**한 뒤
  woff2로 압축해 `fonts/`에 둔다. 서체당 약 0.8 MB.
- `style/book.css`의 `@font-face`가 **파일명만으로** 참조하고,
  `build.sh`가 `--epub-embed-font`로 EPUB에 동봉한다.
- 폴백 스택: Noto Serif KR → 나눔명조 → serif.

### 3.5 텍스트 위계

| 요소 | 서체 | 크기 | 처리 |
|---|---|---|---|
| 본문 `p` | Batang Light | 1em / 행간 1.82 | 양쪽 정렬, 2번째 문단부터 1em 들여쓰기 |
| 부 도비라 `h1.part` | Batang Bold | 1.55em | 상단 22% 여백, 하단 2px accent 괘선, 다음 쪽부터 본문 |
| 장 번호 `.ch-num` | Batang Light | 2.35em | accent 색, 자간 0.14em, 하단 헤어라인 |
| 장 제목 `h1` | Batang Bold | 1.52em | 장마다 새 쪽 (`page-break-before`) |
| 소제목 `h2` | Dotum Bold | 1.2em | 상단 `--rule-2` 헤어라인 |
| 세부 `h3` | Dotum Medium | 1em | 본문과 같은 크기, **본문보다 흐리게 하지 않는다** |

> **위계 규칙:** 하위 제목이 본문보다 작거나 흐려지면 안 된다. 명조(본문)와
> 고딕(제목)의 서체 대비가 크기 대비를 대신한다.

### 3.6 구조 요소

원고에서 pandoc `fenced_divs` / `bracketed_spans`로 호출한다.

| 마크업 | 조판 |
|---|---|
| `# [01]{.ch-num} 제목` | 장 표제 |
| `# 제N부 · … {.part}` + `::: {.part-sub}` | 부 도비라 |
| `::: {.formula}` + `[라벨]{.label}` | 수식 — wash 바탕, 좌측 accent 괘선 |
| `::: {.scene}` + `[라벨]{.label}` + `[…]{.scene-hit}` | 실무 재현 블록 |
| `::: {.worksheet}` | 실전 워크시트 — 서식형, 상단 2px accent |
| `::: {.callout}` | 핵심 정리 — 상하 accent 괘선 |
| `[숫자]{.figure-hl}` | 수치 강조 — 모노, accent |
| `<figure class="diagram">` + 인라인 SVG | 도해 |
| `> 인용` | 버핏·멍거 발언 — 좌측 2px accent, 따옴표 없음 |
| 표 | 헤어라인, 숫자 열 우측 정렬·tabular-nums |

### 3.7 도해 — 원고에서 분리한다

도해는 `books/<슬러그>/figures/<이름>.svg`에 두고, 원고에서는 한 줄로 부른다.

```markdown
<!--figure: context-drift-->
<!--figure: context-drift | 캡션 문장-->
```

`tools/build.sh`가 빌드할 때 `<figure class="diagram">`으로 감싸 끼워 넣는다.
좌표 수십 줄이 원고 한가운데 박히지 않게 하려는 장치이며, **SVG 안의 빈 줄도
이 단계에서 제거**되므로 아래 마지막 규칙은 자동으로 지켜진다.

원고는 건드리지 않는다 — `build/_src/`에 작업 사본을 만들어 pandoc에 넘기고
빌드가 끝나면 지운다.

#### 인라인 SVG 작성 규칙

- 색은 `currentColor` 또는 `.d-accent` / `.d-pct` / `.d-zero` 클래스만 사용한다.
  `.d-*`는 다크 모드에서 자동 반전된다.
- `viewBox` 필수, `width`/`height` 속성 금지 (CSS가 100%로 늘린다).
- `role="img"` + `aria-label`로 대체 텍스트를 반드시 단다.
- **`<svg>` … `</svg>` 안에 빈 줄을 넣지 않는다.** pandoc이 빈 줄에서
  raw HTML 블록을 끊고 내부를 `<p>`로 감싸 도해가 통째로 깨진다.
  (2026-09-02 실제 발생 — 배포 EPUB에까지 반영되었던 사고)

---

## 4. 빌드 파이프라인

### 4.1 의존성

| 도구 | 용도 |
|---|---|
| pandoc 3.x | 마크다운 → EPUB3 |
| Python + fontTools | 서체 서브셋 (`python -m fontTools.subset`) |
| Chrome (선택) | HTML 미리보기 스크린샷 |

### 4.2 명령

```bash
bash tools/new-book.sh <슬러그> "제목" "부제"   # 새 책 뼈대
bash tools/build.sh <슬러그>                    # EPUB + 미리보기 + 리더
bash tools/preview.sh <슬러그> [.md]            # 한 챕터만
bash tools/specimen.sh                          # 조판 견본 (공용)
```

책이 한 권뿐이면 슬러그를 생략할 수 있다.

### 4.3 `tools/build.sh`가 하는 일

1. `books/<슬러그>/manuscript/*.md`를 파일명 오름차순으로 로드.
1-1. 도해 자리표시자를 `figures/*.svg`로 펼쳐 `build/_src/`에 작업 사본 생성.
2. 그 책의 `meta/metadata.yaml`을 앞에 붙여 메타데이터 주입.
3. **공용** `fonts/*.woff2`를 전부 `--epub-embed-font`로 동봉.
4. 그 책의 `images/cover.jpg|png`가 있으면 표지로 자동 인식.
5. `--split-level=1`로 h1마다 별도 XHTML 파일 분할, `--toc-depth=2`로 차례 생성.
6. `--mathml`로 수식 출력.
7. 직접 조판한 표제지(`*표제지*.md`)가 있으면 `--epub-title-page=false`.
8. 책별 `style/override.css`가 있으면 공용 CSS 뒤에 얹는다.
9. 스크롤 미리보기(`preview.html`)와 페이지 넘김 리더(`reader.html`)를 함께 생성.
   리더의 강조색은 그 책의 `override.css`에서 뽑아 주입한다.

### 4.4 `tools/preview.sh`

한 챕터를 `build/preview.html`로 렌더링해 기본 브라우저로 연다.

> **주의:** HTML 미리보기는 **스크롤 문서**라서 `page-break-before`가 무시된다.
> 장 표제면과 부 도비라의 쪽 분리는 **EPUB 리더에서만** 확인할 수 있다.
> 미리보기에서 장이 이어붙어 보이는 것은 정상이다.

---

## 5. 품질 체크리스트

빌드 후 다음을 확인한다.

- [ ] 인라인 SVG가 `<p>`로 감싸지지 않았는가 — `grep '<p><g ' build/*.xhtml`
- [ ] `fonts/*.woff2`가 EPUB 안에 들어갔는가
- [ ] 라이트/다크 양쪽에서 대비가 유지되는가
- [ ] 표가 좁은 화면에서 가로 스크롤되는가 (`.table-wrap`)
- [ ] 각주 왕복 링크가 동작하는가
- [ ] 하위 제목이 본문보다 작거나 흐려지지 않았는가 (§3.5)
- [ ] EPUB 유효성 (epubcheck)

---

## 6. 원고 현황

| 구분 | 파일 | 상태 |
|---|---|---|
| 프롤로그 | `00-프롤로그.md` | 소제목만 |
| 제1부 | `10`–`13` | **제1장 집필 완료.** 2·3장 소제목만 |
| 제2부 | `20`–`23` | 소제목만 |
| 제3부 | `30`–`33` | 소제목만 |
| 제4부 | `40`–`43` | 소제목만 |
| 제5부 | `50`–`52` | 소제목만 |
| 에필로그 | `90-에필로그.md` | 소제목만 |

조판 요소(`.scene` / `.worksheet` / `.callout` / 표 / 도해)를 실제로 사용하는
본문은 현재 **제1장뿐**이다. 나머지 장을 집필하면서 §3.6의 마크업을 적용한다.
